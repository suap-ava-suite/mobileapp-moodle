/** Namespace global compartilhado (window.MobileMoodle). */
export const MM = (window.MobileMoodle = window.MobileMoodle || ({} as MobileMoodleNamespace));
export const App = (MM.App = MM.App || ({} as MobileMoodleApp));
